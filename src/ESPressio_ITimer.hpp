#pragma once

#include <cstdint>
#include <functional>

namespace ESPressio {

    namespace Timing {

        /*
            `ITimer` is a common Interface for all Timer Types provided by this library.
            You can use it to reference any Timer Type without knowing the actual type.
        */
        class ITimer {
            
        };

    }

}